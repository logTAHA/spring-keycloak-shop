info:
  name: Private 404 Path
  type: http
  seq: 3

http:
  method: GET
  url: localhost:8080/api/not-exist-page

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
