info:
  name: Get Product
  type: http
  seq: 5

http:
  method: GET
  url: localhost:8080/api/products/Laptop

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
