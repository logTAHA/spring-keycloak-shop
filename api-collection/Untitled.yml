info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8180/realms/shop/protocol/openid-connect/token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
