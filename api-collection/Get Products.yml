info:
  name: Get Products
  type: http
  seq: 4

http:
  method: GET
  url: localhost:8080/api/products?page=0&size=10&sortBy=PRICE_DESC&inStockOnly=true
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "10"
      type: query
    - name: sortBy
      value: PRICE_DESC
      type: query
    - name: inStockOnly
      value: "true"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
