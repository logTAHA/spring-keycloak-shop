-- Creates the Keycloak database.
-- The application database is created automatically via POSTGRES_DB.
-- For production, create dedicated users per database instead of sharing POSTGRES_USER.
CREATE DATABASE keycloak;
